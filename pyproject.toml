[project]
name = "auvra-host"
version = "0.0.0"
description = "Auvra development host and launcher"
requires-python = ">=3.12,<3.15"
dependencies = [
    "jsonschema>=4.23,<5",
    "pythonnet==3.1.0",
]

[tool.uv]
package = false
required-version = "==0.12.5"
